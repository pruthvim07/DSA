#include <stdio.h>

int main() 
{
    float principal = 1000, rate = 5, time = 2, interest;
    interest = (principal * rate * time) / 100;
    printf("Simple interest = %f\n", interest);
    return 0;
}
