#include <stdio.h>

int main() 
{
    int num = -5;
    if(num > 0)
        printf("%d is positive\n", num);
    else if(num < 0)
        printf("%d is negative\n", num);
    else
        printf("It's zero\n");
    return 0;
}
