#include <stdio.h>

int main()
{
    float length = 5, width = 3;
    float area = length * width;
    printf("Area of rectangle with length %f and width %f = %f\n", length, width, area);
    return 0;
}
