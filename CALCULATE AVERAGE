#include <stdio.h>

int main() 
{
    int numbers[5] = {5, 10, 15, 20, 25}, sum = 0;
    float average;
    for (int i = 0; i < 5; ++i)
        sum += numbers[i];
    average = (float)sum / 5;
    printf("Average = %.2f\n", average);
    return 0;
}
